package company.plans.device;

import company.plans.Plan;

public class DeviceS extends Plan {
    public DeviceS(double price, int usersNumber) {
        super("Device S", price, usersNumber);
    }
}
